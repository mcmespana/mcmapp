name: CI

on:
  pull_request:
    branches: [main, production, preview]

jobs:
  # Delega en el workflow reutilizable para no mantener dos listas de pasos que
  # se separan con el tiempo (los workflows de OTA y de web llaman al mismo).
  ci:
    uses: ./.github/workflows/verify.yml

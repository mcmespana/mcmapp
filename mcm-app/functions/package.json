{
  "name": "mcm-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "npm run sync:parser && tsc",
    "build:watch": "npm run sync:parser && tsc -w",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "sync:parser": "node scripts/sync-ics-parser.mjs"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}

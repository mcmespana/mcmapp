## 2026-06-16 - [ListHeaderComponent React Element Memory Leak]
**Learning:** [Defining a React Element for \`ListHeaderComponent\` directly inside the parent render loop without memoization causes the `FlatList` to unmount and remount the header entirely on every parent re-render (like keystrokes in a search input). This also forces the \`FlatList\` internal layout mechanics to recalculate.]
**Action:** [Always wrap \`ListHeaderComponent\` elements defined inside functional components with \`useMemo\`, or extract them into separate memoized components outside the main render loop.]
\n## 2024-07-05 - [Object.entries() inside useMemo]\n**Learning:** [Using `Object.entries(largeObject)` inside heavily executed `useMemo` hooks creates significant Garbage Collection (GC) pressure by generating temporary arrays of tuple arrays `[key, value][]` on every dependency change. For massive state objects like `allSongsData` containing the full catalog, this led to measurable frame drops during list renders and data swaps.]\n**Action:** [Use native `for...in` loops combined with an `Object.prototype.hasOwnProperty.call(obj, key)` check to iterate through large objects efficiently with 0 allocations, safely replicating the behavior of `Object.entries()` without the GC overhead.]
